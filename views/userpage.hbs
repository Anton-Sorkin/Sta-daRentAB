{{>nav}}

<div class="userpage-container">


    <div class="userpage-booking">
        {{!-- <h2 id="userpage-booktag">Boka en städning </h2>
        <form>
            <div class="userpage-date">
                <label id="userpage-date">Datum</label>
            </div>
            <input type="date">
        </form> --}}
        <h2 id="userpage-booktag">Boka en städning </h2>
        <form action="/user/dateselect" method="post">
            <div class="userpage-date">
                <label for="date" id="userpage-date">Datum</label>
            </div>
            <input type="date" name="date" id="date">
            <input type="submit" value="Boka" id="bokabtn">
        </form>
    </div>


    <div class="userpage-yourCleanings">
        <h2 id="your-cleanings">Dina städningar</h2>

        <div class="userpage-links">
            <ul>
                {{#each userCleanings}}
                {{#if done}}
                {{else}}
                <li><a href="user/single/{{_id}}">{{cleaningType}} {{date}} <button
                            id="userpage-details">Detaljer</button></a></li>
                {{/if}}
                {{/each}}
            </ul>
        </div>
    </div>



    <div class="userpage-cleaningHistory">
        <h2 id="your-oldCleanings">Din Städhistorik</h2>

        <div id="oldcleanings-list">
            {{#each userCleanings}}
            {{#if done}}
            <p>{{cleaningType}} {{date}}</p>
            {{/if}}
            {{/each}}
        </div>
    </div>
</div>